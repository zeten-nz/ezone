// Mirrors ezone-server/config/validation.js — kept as a separate literal
// since the frontend and backend are different JS runtimes and can't share
// a module across the client/server boundary.
export const PHONE_REGEX = /^\+998\d{9}$/;
